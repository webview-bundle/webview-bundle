# create-wvb

Scaffold a [Webview Bundle](https://wvb.dev) app — a web app packed into a single `.wvb` archive and
served from a native webview host, offline-first, with over-the-air updates.

```sh
npm create wvb@latest
# or
pnpm create wvb
yarn create wvb
bun create wvb
```
