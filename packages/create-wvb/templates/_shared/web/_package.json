{
  "name": "{{bundleName}}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@wvb/bridge": "{{wvbVersion:@wvb/bridge}}"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.1.5"
  }
}
