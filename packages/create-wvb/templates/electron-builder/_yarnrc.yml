# The scripts run the web workspace with `npm --prefix web`, which needs a real node_modules
# tree. Yarn 2+ defaults to the PnP linker (no node_modules), so pin the node-modules linker.
nodeLinker: node-modules
